#include <stdio.h>
#include <stdlib.h>
int test();
char filename[128] = "0";
int engine(){
    return test();







    /*while (size_of_file > 0)
    {
        if (not_success){
            size_of_file << 2;
            not_success = 0;
        }
        sprintf(filename, "%d", number_of_files);
        file = fopen(filename, "r");

    }*/
    
}
